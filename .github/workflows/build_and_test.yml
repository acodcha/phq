name: build and test
on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]
jobs:
  build:
    name: build and run tests
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: create build directory
      run: mkdir -p build
    - name: configure
      run: cd build && cmake .. -DPHQ_TEST=ON
    - name: build
      run: cd build && make -j
    - name: test
      run: cd build && make test

